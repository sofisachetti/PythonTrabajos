# Definicion de la funcion

def sumar(a, b):
    resultado = a + b # Calcula la suma
    return resultado # Devuelve el resultado

# Llamo a la funcion y le paso argumentos
suma_total = sumar(3, 4)

print(f"La suma es: {suma_total}.")